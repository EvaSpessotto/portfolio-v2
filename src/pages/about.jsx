import React, { Component } from "react"
import Layout from "../components/Layout"

class about extends Component {
  render() {
    return <Layout>Hello, aboutpage</Layout>
  }
}

export default about
