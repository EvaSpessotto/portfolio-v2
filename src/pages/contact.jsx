import React, { Component } from "react"
import Layout from "../components/Layout"

class contact extends Component {
  render() {
    return <Layout>Hello, contact page</Layout>
  }
}

export default contact
